# On Day 1 i learned to:
1. Install WSL
2. what a shell actually is and recognise its prompt(user@laptop:-$) within wsl
3. Open and run commands on both windows and ubuntu powershell
4. navigate to c and d drives using commands
